<h1>予約の確認</h1>

<p><strong>施設名:</strong> <%= @room.name %></p>
<p><strong>詳細:</strong> <%= @room.description %></p>
<p><strong>料金:</strong> <%= number_to_currency(@room.price) %></p>
<p><strong>住所:</strong> <%= @room.address %></p>

<%= image_tag @room.image if @room.image.attached? %>

<%= link_to 'この内容で予約する', finalize_reservation_room_path(@room), method: :post, class: 'button' %>
<%= link_to '編集', edit_room_path(@room), class: 'button' %>
<%= link_to '戻る', new_room_path, class: 'button' %>
