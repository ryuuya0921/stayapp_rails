<!DOCTYPE html>
<html>
  <head>
    <title>Stay</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
  <header>
    <h1><%= link_to '宿泊予約アプリ', root_path %></h1>
    <% if current_user %>
      <p><%= current_user.name %></p>
      <%= link_to 'ログアウト', logout_path, method: :delete %>
    <% else %>
      <p><%= link_to 'ログイン', login_path %></p>
      <p><%= link_to '新規登録', new_user_path %></p>
    <% end %>

    <%# rooms/new %>
    <p>施設の新規登録</p> 
    <%# #reservations/index %>
    <p>予約済み一覧</p>
    <%# rooms/index %>
    <p>登録済み一覧</p>
    <%# users/ controller設定 %>
    <% if current_user %>
      <p><%= link_to 'アカウント設定', account_settings_user_path(current_user) %></p>
    <% end %>


  </header>
  </body>
  <%= javascript_include_tag 'rails-ujs' %>

</html>
